# Web 框架
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 控制面存储
sqlalchemy==2.0.23
pydantic==2.5.2
pydantic-settings==2.1.0

# 认证与加密
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==41.0.7

# MySQL 同步与备份
mysql-connector-python==8.2.0
pymysql==1.1.0
# 必须 >=1.0.7：1.0.5/1.0.6 的 binlogstream.py 会调用 logging.WARN(...)，
# 该符号在 Python 3 是整数常量 30，一旦源库掐断空闲连接（OperationalError
# 2006/2013，库本该自动重连的正常路径）就抛 TypeError，直接打死同步线程。
# 启动期有版本守卫兜底（见 app/services/sync/engine.py 的 _assert_reader_lib_sane）。
mysql-replication==1.0.17

# OpenList 备份上传（运行时依赖）+ 测试客户端
httpx==0.25.2

# 调度与可观测
APScheduler==3.10.4
loguru==0.7.2
psutil==5.9.7

# 测试（开发环境）
pytest==9.1.1
